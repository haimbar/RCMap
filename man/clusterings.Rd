% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RCMap.R
\name{clusterings}
\alias{clusterings}
\title{Run MDS and build co-membership matrices for Euclidean and hyperbolic distances.}
\usage{
clusterings(adjmat, j = 0, max_nc = 3, clustMethod = "ward.D2")
}
\arguments{
\item{adjmat}{A list of n SxS adjacency matrices (one per sorter).}

\item{j}{Index of the sorter to leave out (0 = use all sorters, default).
If \code{j} exceeds the number of sorters it is silently reset to 0.}

\item{max_nc}{Maximum number of clusters to consider (upper bound for k).}

\item{clustMethod}{Hierarchical clustering method passed to \code{hclust}
(default \code{"ward.D2"}).}
}
\value{
A named list with two elements: \code{CmatE} and \code{CmatH}, each
  a list of \code{max_nc - 1} co-membership matrices (see \code{get_cmat})
  based on Euclidean and hyperbolic 2-D distances respectively.
}
\description{
Optionally drops one sorter (leave-one-out), computes the dissimilarity
matrix, runs 2-D MDS, then calls \code{get_cmat} for both Euclidean and
hyperbolic distance metrics across all cluster solutions up to \code{max_nc}.
}
